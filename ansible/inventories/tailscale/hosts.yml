---
# Connectivity override layered after the private topology inventory.
# Tailscale MagicDNS resolves these names only on an enrolled operator device.
all:
  hosts:
    k3s-01:
      ansible_host: k3s-01
    k3s-02:
      ansible_host: k3s-02
    k3s-03:
      ansible_host: k3s-03
