---
- name: Sysctl 설정 다시 읽기
  ansible.builtin.command: sysctl --system
  changed_when: true

- name: WireGuard 재시작
  ansible.builtin.systemd_service:
    name: "wg-quick@{{ wireguard_interface }}"
    state: restarted
