---
# 전체 재구축과 종료 Runbook의 리허설용.
# TODO: gabiactl destroy 전 노드 내부 정리 절차 구현
- name: Teardown (재구축 리허설)
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: TODO — teardown 절차 구현
      ansible.builtin.debug:
        msg: "TODO: docs/runbooks/full-rebuild.md의 노드 내부 정리 절차 구현"
