---
# 프로덕션 Gate 자동 검증. docs/roadmap.md의 판정 항목을 assert로 표현한다.
# TODO: 노드 Ready, etcd, Cilium, 자원 Gate를 구현한다.
- name: Gate 검증
  hosts: k3s_first_server
  become: true
  gather_facts: false
  tasks:
    - name: TODO — 노드 3대 Ready 확인
      ansible.builtin.debug:
        msg: "TODO: kubectl get nodes 로 3대 Ready 검증"

    - name: TODO — etcd 멤버 3개 확인
      ansible.builtin.debug:
        msg: "TODO: journalctl -u k3s | grep etcd.*member"

    - name: TODO — cilium connectivity test
      ansible.builtin.debug:
        msg: "TODO: cilium connectivity test 실행 및 결과 검증"
