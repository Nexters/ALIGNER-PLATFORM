---
collections:
  - name: kubernetes.core
    version: "6.4.0"        # ansible-core 2.16+; helm/kubeconfig 모듈에 사용
  - name: ansible.posix
    version: "2.1.0"        # ansible-core 2.16+; mount, sysctl 등
  - name: community.general
    version: "9.5.1"        # filesystem 모듈 — storage role의 안전한 포맷에 사용
