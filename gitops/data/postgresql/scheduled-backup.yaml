apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: aligner-db-weekly
  namespace: aligner-data
spec:
  schedule: "0 0 0 * * 0"
  backupOwnerReference: none
  cluster:
    name: aligner-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
