apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Issuers and Certificates are runtime-gated because ACME cannot validate a
# placeholder hostname or account email. See runtime/README.md.
resources: []
