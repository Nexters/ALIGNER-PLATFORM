apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster-issuers.yaml
  - staging-certificate.yaml
  # Add production-certificate.yaml only after the staging Certificate is Ready.
