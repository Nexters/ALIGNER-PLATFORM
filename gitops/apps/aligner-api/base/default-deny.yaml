apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: aligner-api-default-deny
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: aligner-api
  policyTypes:
    - Ingress
    - Egress
